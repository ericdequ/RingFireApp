import React from 'react';
import { StyleSheet, Text } from 'react-native';

const FireText = ({ title }) => {
  return (
    <Text style={styles.fireText}>{title}</Text>
  );
};


export default FireText;
